#include "main.h"
/**
 * mul - writes the character c to stdout
 * @a: The character to mul
 * @b: The character to mul
 * Return: mltpl.
 */
int mul(int a, int b)
{
	int mltpl;

	mltpl = a * b;
	return (mltpl);
}
