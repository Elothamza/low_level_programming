#include "main.h"
/**
 *add - Entry point
 *@num1: the first number.
 *@num2: the second number.
 *Explain: 'add two integers and returns the result.'
 *Return: Always sum (Success)
 */
int add(int num1, int num2)
{
	int sum;

	sum = num1 + num2;
	return (sum);
}
